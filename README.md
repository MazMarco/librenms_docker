# librenms_with_docker
librenms docker compose

on clean vps run:

```bash
bash <(curl -fsSL https://raw.githubusercontent.com/MazMarco/librenms_docker/main/install.sh)
```
it will install and configure docker + librenms
